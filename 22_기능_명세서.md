# 기능 명세서 — QT-AI

> **문서 버전:** v1.1
> **작성일:** 2026-05-12
> **기준 문서:** `05_요구사항_정의서.md` v1.3, `DECISIONS.md`, `apis/{ai,bff,bible}/openapi.yaml`, Notion `관리자 기능 명세`
> **목적:** 요구사항을 화면·API·데이터·예외·완료 기준 단위로 내려 구현자가 바로 작업할 수 있는 기능 명세를 제공한다.

---

## 변경 이력

| 버전 | 날짜 | 변경 내용 |
| --- | --- | --- |
| v1.0 | 2026-05-12 | 2차 회의 후 확정된 요구사항 기준 최초 작성. 소프트 로그인, DeepSeek 1회성 Q&A, 묵상 4필드, 선택적 익명 나눔, Bible Service 묵상 통합 반영 |
| v1.1 | 2026-05-12 | Notion 관리자 기능 명세 반영. 관리자 P0/P1/P2 범위, AI 로그 조회, 신고/삭제, 감사 로그 항목 보강 |

---

## 1. 기능 명세 원칙

### 1.1 사용자 경험 원칙

- 첫 진입에서 로그인을 강제하지 않는다.
- 사용자는 오늘의 QT 본문을 먼저 읽고, 필요할 때 설명·주석·AI·묵상 기록으로 확장한다.
- 개인 묵상 기록은 기본 비공개다.
- 공유는 사용자의 명시적 선택이 있을 때만 동작한다.
- AI는 사용자의 신앙 상태를 평가하지 않고, 본문 이해와 묵상을 돕는 방향으로만 답한다.
- 설명·주석·AI 답변은 출처를 함께 보여준다.

### 1.2 구현 기준

- API 상세 스키마는 `apis/{service}/openapi.yaml`을 우선한다.
- Gateway 외부 경로와 인증 정책은 `DECISIONS.md`를 우선한다.
- 묵상 기록 API는 별도 Journal Service가 아니라 Bible Service에 포함한다.
- Auth Service와 Journal Service는 MVP에서 독립 서비스로 만들지 않는다.
- 성경 데이터는 개역한글, KJV, Matthew Henry 주석만 허용한다.
- 개역개정, ESV, NIV, 찬양 가사·음원 저장은 금지한다.

### 1.3 공통 상태 및 권한

| 구분 | 정책 |
| --- | --- |
| 비로그인 허용 | 온보딩, QT 가이드, 오늘의 QT 미리보기, 한/영 성경 본문, 성경 목록, 쉬운 본문 설명 |
| 로그인 필요 | 주석 열람, AI 질문, 묵상 기록 저장·수정·삭제, 찬양 목록 저장, 익명 나눔 공개·취소, 좋아요, 댓글 작성, 신고 |
| 선택 인증 | `GET /api/v1/qt/today`, `GET /api/v1/passages/{bookCode}/{chapter}/{verse}` |
| 기본 공개 상태 | `PRIVATE` |
| 공유 공개 상태 | `ANONYMOUS_SHARED` |
| AI 세션 상태 | `IN_PROGRESS`, `COMPLETED`, `COMPLETED_NO_JOURNAL` |

---

## 2. 기능 목록

| ID | 기능 | 우선순위 | 담당 영역 | 대표 화면 |
| --- | --- | --- | --- | --- |
| FS-01 | 온보딩·QT 가이드 | 필수 | Flutter | 온보딩, QT 가이드 |
| FS-02 | 홈·소프트 로그인 | 필수 | Flutter, Gateway, BFF | 홈 |
| FS-03 | 오늘의 QT·입체 본문 보기 | 필수 | Flutter, BFF, Bible | 오늘의 QT |
| FS-04 | 쉬운 본문 설명·용어 설명 | 필수 | Flutter, Bible, BFF | 오늘의 QT |
| FS-05 | 주석 보기 | 필수 | Flutter, Bible, BFF | 오늘의 QT |
| FS-06 | AI 질문·출처 기반 1회성 Q&A | 필수 | Flutter, AI | AI 질문 |
| FS-07 | 묵상 기록 관리 | 필수 | Flutter, Bible, Kafka | 개인 기록 |
| FS-08 | 선택적 익명 나눔 | 필수 | Flutter, Bible, BFF | 익명 나눔 |
| FS-09 | 찬양 추천·내 찬양 목록 | 중요/보류 | Flutter, BFF 확장 | 내 찬양 |
| FS-10 | 알림·관리자 운영 | 중요 | BFF, Flutter, Bible, AI | 알림, 관리자 |

---

## 3. 상세 기능 명세

## FS-01. 온보딩·QT 가이드

### 목적

QT를 처음 접하는 사용자가 앱을 켰을 때 무엇을 해야 하는지 바로 이해하도록 돕는다.

### 진입 조건

| 항목 | 내용 |
| --- | --- |
| Actor | 비로그인 사용자, 로그인 사용자 |
| 진입점 | 앱 최초 실행, 홈의 `QT 가이드` |
| 인증 | 불필요 |

### 화면 구성

| 영역 | 표시 내용 |
| --- | --- |
| 첫 문구 | `QT가 처음이신가요?` |
| 설명 | QT의 의미, 오늘의 본문 읽기, 질문하기, 묵상 기록하기 순서 |
| CTA | `오늘의 QT 시작하기` |
| 보조 진입 | `나중에 보기`, `QT 용어 보기` |

### 동작 흐름

1. 앱 최초 실행 시 온보딩 완료 여부를 로컬 저장소에서 확인한다.
2. 완료 이력이 없으면 온보딩을 표시한다.
3. 사용자가 `오늘의 QT 시작하기`를 누르면 오늘의 QT 화면으로 이동한다.
4. 사용자가 `나중에 보기`를 누르면 홈으로 이동하되, 홈에서 다시 QT 가이드로 접근할 수 있다.

### 데이터

| 필드 | 저장 위치 | 설명 |
| --- | --- | --- |
| `onboardingSeen` | Flutter local storage | 온보딩 확인 여부 |
| `guideTerms` | 앱 정적 데이터 또는 Bible Service | QT, 주석, 묵상, 적용, 기도 제목 등 용어 |

### 예외

| 상황 | 처리 |
| --- | --- |
| 로컬 저장소 읽기 실패 | 온보딩을 다시 표시한다 |
| 용어 데이터 로딩 실패 | 기본 내장 용어 목록을 표시한다 |

### 완료 기준

- 첫 실행 시 온보딩이 표시된다.
- 로그인하지 않아도 온보딩과 QT 가이드를 볼 수 있다.
- 온보딩 완료 후 재실행 시 홈으로 진입한다.

---

## FS-02. 홈·소프트 로그인

### 목적

로그인 전에도 핵심 경험을 미리 볼 수 있게 하고, 기록·AI·공유처럼 개인 데이터가 필요한 시점에만 로그인을 유도한다.

### 진입 조건

| 항목 | 내용 |
| --- | --- |
| Actor | 비로그인 사용자, 로그인 사용자 |
| 진입점 | 앱 실행, 온보딩 완료 |
| 인증 | 선택 |

### 화면 구성

| 메뉴 | 비로그인 | 로그인 |
| --- | --- | --- |
| 오늘의 QT 시작하기 | 가능 | 가능 |
| 내 묵상 기록 | 로그인 유도 | 가능 |
| 성경 검색 | 가능 | 가능 |
| AI에게 질문하기 | 로그인 유도 | 가능 |
| 내 찬양 목록 | 로그인 유도 | 가능 |
| QT 가이드 | 가능 | 가능 |
| 익명 나눔 | 목록 읽기 가능, 작성·반응은 로그인 유도 | 가능 |

### API

| 목적 | Method | Path | 인증 |
| --- | --- | --- | --- |
| 대시보드 | GET | `/api/v1/me/dashboard` | 필수 |
| 오늘의 QT 미리보기 | GET | `/api/v1/qt/today` | 선택 |
| 로그인 | POST | `/auth/login` 또는 Gateway Auth 경로 | 불필요 |
| Google OAuth | GET/POST | Gateway Auth 경로 | 불필요 |

### 동작 흐름

1. 앱 실행 시 토큰 존재 여부를 확인한다.
2. 토큰이 있으면 사용자 대시보드와 오늘의 QT를 함께 로딩한다.
3. 토큰이 없으면 오늘의 QT 미리보기와 공개 메뉴만 표시한다.
4. 사용자가 로그인 필요 기능을 누르면 로그인 화면으로 이동한다.
5. 로그인 완료 후 원래 진입하려던 화면으로 복귀한다.

### 예외

| 상황 | 처리 |
| --- | --- |
| Access Token 만료 | Refresh Token으로 재발급 후 원 요청 재시도 |
| Refresh 실패 | 로그인 상태 초기화 후 로그인 화면 이동 |
| 대시보드 실패 | 홈 기본 메뉴는 유지하고 오류 메시지 표시 |

### 완료 기준

- 비로그인 첫 진입에서 홈과 오늘의 QT 시작 진입점이 보인다.
- 로그인 필요 기능은 누르는 순간 자연스럽게 로그인으로 안내한다.
- 로그인 후 원래 목적 화면으로 복귀한다.

---

## FS-03. 오늘의 QT·입체 본문 보기

### 목적

사용자가 오늘의 성경 본문을 절별 한글·영어 형태로 읽고, 한 흐름 안에서 설명·주석·AI·묵상으로 이어가도록 한다.

### 진입 조건

| 항목 | 내용 |
| --- | --- |
| Actor | 비로그인 사용자, 로그인 사용자 |
| 진입점 | 홈의 `오늘의 QT 시작하기`, 성경 검색 결과 |
| 인증 | 선택 |

### 화면 구성

| 영역 | 표시 내용 |
| --- | --- |
| 본문 헤더 | 본문 범위, 날짜, 주제 |
| 본문 리스트 | 절 번호, 개역한글, KJV |
| 설명 패널 | 한 줄 요약, 쉬운 설명, 어려운 단어 |
| 주석 패널 | Matthew Henry 원문 또는 요약 더미 |
| 액션 | AI 질문하기, 묵상 기록하기, 찬양 보기 |

### API

| 목적 | Method | Path | 인증 |
| --- | --- | --- | --- |
| 오늘의 QT | GET | `/api/v1/qt/today` | 선택 |
| 통합 본문 | GET | `/api/v1/passages/{bookCode}/{chapter}/{verse}` | 선택 |
| 성경 목록 | GET | `/bible/books` | 불필요 |
| 한글 본문 | GET | `/bible/kr/{bookCode}/{ch}/{v}` | 불필요 |
| 영어 본문 | GET | `/bible/en/{bookCode}/{ch}/{v}` | 불필요 |

### 요청 옵션

| 파라미터 | 설명 |
| --- | --- |
| `includeCommentary` | 로그인 사용자가 주석 포함 여부를 선택한다. 기본값은 false |

### 동작 흐름

1. 사용자가 오늘의 QT를 시작한다.
2. BFF가 오늘의 본문과 필요한 본문 데이터를 병렬 조회한다.
3. 화면은 한글·영어 본문을 절 단위로 묶어서 표시한다.
4. 사용자가 스크롤하면 한글·영어 절 위치가 함께 움직인다.
5. 사용자가 설명·주석·AI·묵상 액션을 선택하면 해당 기능으로 이어진다.

### 데이터 표시 규칙

| 규칙 | 내용 |
| --- | --- |
| 절 정렬 | 한글 본문과 영어 본문은 같은 절 번호 기준으로 묶는다 |
| 본문 누락 | 한쪽 번역이 없으면 해당 절에 빈 상태 메시지를 표시한다 |
| 저작권 | 개역개정, ESV, NIV는 표시하지 않는다 |
| 출처 | 본문 하단에 데이터 출처를 표시한다 |

### 예외

| 상황 | 처리 |
| --- | --- |
| 일부 본문 조회 실패 | 성공한 본문을 먼저 표시하고 실패 영역만 재시도 버튼 제공 |
| 오늘의 QT 데이터 없음 | 기본 샘플 본문 또는 성경 검색으로 유도 |
| 비로그인 주석 열람 | 로그인 유도 |

### 완료 기준

- 절별 한글·영어 본문이 같은 절 기준으로 표시된다.
- 비로그인 사용자도 본문과 쉬운 설명까지 볼 수 있다.
- 주석은 사용자가 선택했을 때만 로딩한다.

---

## FS-04. 쉬운 본문 설명·용어 설명

### 목적

성경 지식이 많지 않은 사용자가 본문을 어렵게 느끼지 않도록 요약, 배경, 단어 설명을 제공한다.

### 진입 조건

| 항목 | 내용 |
| --- | --- |
| Actor | 비로그인 사용자, 로그인 사용자 |
| 진입점 | 오늘의 QT 화면의 설명 패널 |
| 인증 | 불필요 |

### 화면 구성

| 영역 | 표시 내용 |
| --- | --- |
| 한 줄 요약 | 오늘 본문 핵심을 한 문장으로 표시 |
| 쉬운 설명 | 초심자 기준의 배경과 맥락 |
| 어려운 단어 | 본문 안의 낯선 단어와 쉬운 뜻 |
| QT 용어 | QT, 주석, 묵상, 적용, 기도 제목 등 |

### API

| 목적 | Method | Path | 인증 |
| --- | --- | --- | --- |
| 쉬운 설명 | GET | `/api/v1/explanations/{bookCode}/{ch}/{v}` | 불필요 |
| 통합 본문 내 설명 | GET | `/api/v1/passages/{bookCode}/{chapter}/{verse}` | 선택 |

### 데이터

| 필드 | 설명 |
| --- | --- |
| `summary` | 본문 한 줄 요약 |
| `background` | 본문 배경 설명 |
| `terms` | 어려운 단어 목록 |
| `source` | 설명 생성 또는 검수 출처 |

### 예외

| 상황 | 처리 |
| --- | --- |
| 설명 데이터 없음 | `아직 준비 중인 설명입니다` 표시 |
| 용어 데이터 없음 | 앱 내 기본 용어 사전 사용 |

### 완료 기준

- 설명은 기본 본문을 가리지 않고 선택적으로 열린다.
- 용어는 초심자 기준의 쉬운 문장으로 제공된다.
- AI가 매번 즉시 생성한 내용처럼 표시하지 않는다.

---

## FS-05. 주석 보기

### 목적

본문을 더 깊게 이해하고 싶은 사용자가 Matthew Henry 주석 또는 검수된 더미 주석을 선택적으로 볼 수 있게 한다.

### 진입 조건

| 항목 | 내용 |
| --- | --- |
| Actor | 로그인 사용자 |
| 진입점 | 오늘의 QT 화면의 `주석 보기` |
| 인증 | 필수 |

### API

| 목적 | Method | Path | 인증 |
| --- | --- | --- | --- |
| 주석 조회 | GET | `/api/v1/commentary/{bookCode}/{ch}/{v}` | 필수 |
| 통합 본문 주석 포함 조회 | GET | `/api/v1/passages/{bookCode}/{chapter}/{verse}?includeCommentary=true` | 선택 인증 중 로그인 필요 |

### 동작 흐름

1. 사용자가 `주석 보기`를 누른다.
2. 비로그인 상태면 로그인 화면으로 유도한다.
3. 로그인 상태면 주석 데이터를 로딩한다.
4. 주석은 본문 아래 또는 별도 패널로 표시한다.
5. 주석 출처와 저작권 상태를 함께 표시한다.

### 예외

| 상황 | 처리 |
| --- | --- |
| 주석 데이터 없음 | `이 구절의 주석은 준비 중입니다` 표시 |
| 권한 없음 | 로그인 유도 또는 401 처리 |
| 주석 응답 지연 | 설명·본문은 유지하고 주석 패널만 로딩 상태 표시 |

### 완료 기준

- 주석은 사용자가 요청하기 전 자동 노출하지 않는다.
- 비로그인 사용자는 주석 진입 시 로그인으로 안내된다.
- 주석 데이터 출처가 화면에 표시된다.

---

## FS-06. AI 질문·출처 기반 1회성 Q&A

### 목적

사용자가 본문을 읽다가 생긴 질문을 AI에게 묻고, 성경 본문·주석·RAG 출처에 기반한 답을 스트리밍으로 받는다.

### 진입 조건

| 항목 | 내용 |
| --- | --- |
| Actor | 로그인 사용자 |
| 진입점 | 오늘의 QT 화면의 `AI에게 질문하기` |
| 인증 | 필수 |

### 핵심 정책

- AI는 DeepSeek API를 사용한다.
- AI는 계속 이어지는 자유 챗봇이 아니라 출처 기반 1회성 Q&A로 동작한다.
- QT A~D는 자동 진행 단계가 아니라 답변 톤과 질문 가이드 구조로 사용한다.
- 사용자가 `묵상 완료`를 눌러야 AI 세션 완료 이벤트가 발행된다.
- AI 답변 하단에는 RAG 출처를 표시한다.

### API

| 목적 | Method | Path | 인증 |
| --- | --- | --- | --- |
| 세션 생성 | POST | `/ai/sessions` | 필수 |
| 질문 전송·SSE 응답 | POST | `/ai/sessions/{id}/turns` | 필수 |
| 세션 조회 | GET | `/ai/sessions/{id}` | 필수 |
| 세션 목록 | GET | `/ai/sessions` | 필수 |
| 묵상 완료 | POST | `/ai/sessions/{id}/complete` | 필수 |

### SSE 이벤트

| 이벤트 | 처리 |
| --- | --- |
| `turn_started` | 응답 영역을 생성하고 로딩 상태를 표시한다 |
| `token` | 토큰을 누적해 답변 본문에 표시한다 |
| `rag_sources` | 답변 하단 출처 영역에 표시한다 |
| `turn_completed` | 입력창을 다시 활성화하고 완료 CTA를 표시한다 |
| `done` | 스트림을 종료한다. 필요 시 data 값으로 `[DONE]` 마커를 함께 보낼 수 있다 |
| `error` | 스트림을 닫고 재시도 버튼을 표시한다 |

### 화면 구성

| 영역 | 표시 내용 |
| --- | --- |
| 본문 맥락 | 질문 대상 본문 범위 |
| 질문 입력 | 사용자의 질문 |
| AI 답변 | 스트리밍 토큰 누적 표시 |
| 출처 | 본문, 주석, RAG 문서 |
| 완료 CTA | `묵상 완료` |

### 동작 흐름

1. 사용자가 AI 질문 화면에 진입한다.
2. AI 세션이 없으면 `POST /ai/sessions`로 세션을 만든다.
3. 사용자가 질문을 입력한다.
4. 앱은 `POST /ai/sessions/{id}/turns`를 호출하고 SSE를 구독한다.
5. AI 답변이 토큰 단위로 표시된다.
6. 출처가 도착하면 답변 하단에 표시한다.
7. 사용자가 `묵상 완료`를 누르면 `POST /ai/sessions/{id}/complete`를 호출한다.
8. AI Service는 `ai.session.completed` Kafka 이벤트를 발행한다.
9. Bible Service는 이벤트를 소비해 묵상 DRAFT를 생성한다.

### 예외

| 상황 | 처리 |
| --- | --- |
| 비로그인 | 로그인 유도 |
| DeepSeek 응답 지연 | 5초 이상 첫 토큰이 없으면 안내 메시지 표시 |
| SSE 중단 | 부분 답변 유지, 재시도 제공 |
| RAG 출처 없음 | 출처 없음 안내 후 답변 신뢰도 주의 메시지 표시 |
| 완료 이벤트 실패 | 세션 상태 확인 후 재시도 버튼 제공 |

### 완료 기준

- 질문에 대한 답변이 SSE로 스트리밍된다.
- 답변 하단에 출처가 표시된다.
- `묵상 완료` 후 묵상 기록 DRAFT 생성 흐름으로 이어진다.

---

## FS-07. 묵상 기록 관리

### 목적

사용자가 오늘 본문을 읽고 느낀 점을 4개 필드로 정리해 개인 공간에 저장한다.

### 진입 조건

| 항목 | 내용 |
| --- | --- |
| Actor | 로그인 사용자 |
| 진입점 | 오늘의 QT, AI 세션 완료, 내 묵상 기록 |
| 인증 | 필수 |

### 기록 필드

| 필드 | 화면 라벨 | 필수 여부 | 설명 |
| --- | --- | --- | --- |
| `felt` | 오늘 느낀 점 | 선택 | 본문을 읽고 느낀 점 |
| `memorableVerse` | 기억하고 싶은 구절 | 선택 | 마음에 남은 구절 |
| `application` | 내 삶에 적용할 점 | 선택 | 오늘 실천할 내용 |
| `prayer` | 기도 제목 | 선택 | 기도문 또는 기도 제목 |
| `visibility` | 공개 여부 | 필수 | `PRIVATE`, `ANONYMOUS_SHARED` |

### API

| 목적 | Method | Path | 인증 |
| --- | --- | --- | --- |
| 목록 조회 | GET | `/api/v1/journals` | 필수 |
| 상세 조회 | GET | `/api/v1/journals/{id}` | 필수 |
| 수정 | PATCH | `/api/v1/journals/{id}` | 필수 |
| 삭제 | DELETE | `/api/v1/journals/{id}` | 필수 |
| 이벤트 로그 | GET | `/api/v1/journals/{id}/events` | 필수 |

### 생성 정책

- 클라이언트는 `POST /api/v1/journals`를 호출하지 않는다.
- 묵상 기록 DRAFT는 AI Service의 `ai.session.completed` 이벤트를 Bible Service가 소비해 생성한다.
- AI를 사용하지 않은 수동 기록 생성이 필요하면 v1.1에서 별도 정책으로 검토한다.

### 상태

| 상태 | 설명 |
| --- | --- |
| `DRAFT` | AI 세션 완료 후 자동 생성된 초안 |
| `PUBLISHED` | 사용자가 내용을 저장 또는 발행한 상태 |
| `DELETED` | 사용자 삭제 또는 소프트 삭제 상태 |

### 동작 흐름

1. 사용자가 AI 세션에서 `묵상 완료`를 누른다.
2. Bible Service가 DRAFT 묵상 기록을 생성한다.
3. 사용자는 개인 기록 화면에서 4개 필드를 작성한다.
4. 사용자가 저장하면 `PATCH /api/v1/journals/{id}`를 호출한다.
5. 저장 후 기본 공개 상태는 `PRIVATE`를 유지한다.
6. 사용자가 원할 때만 FS-08 흐름으로 공유한다.

### 이벤트

| 이벤트 | 발행 조건 |
| --- | --- |
| `journal.created` | 묵상 DRAFT 생성 완료 |
| `journal.updated` | 묵상 수정 완료 |
| `journal.deleted` | 묵상 삭제 완료 |
| `journal.creation.failed` | AI 완료 후 묵상 생성 실패 |

### 예외

| 상황 | 처리 |
| --- | --- |
| DRAFT 생성 지연 | `묵상 기록을 준비 중입니다` 상태 표시 |
| DRAFT 생성 실패 | 재시도 또는 고객 안내 메시지 표시 |
| 본인 기록 아님 | 403 처리 |
| 저장 중 충돌 | 최신 데이터를 다시 불러오고 사용자에게 재저장 안내 |

### 완료 기준

- AI 완료 후 묵상 DRAFT가 자동 생성된다.
- 묵상 기록은 4개 필드로 수정할 수 있다.
- 모든 기록은 기본 `PRIVATE`다.
- 생성·수정·삭제 이벤트가 Kafka로 발행된다.

---

## FS-08. 선택적 익명 나눔

### 목적

사용자가 직접 선택한 묵상 기록만 익명으로 조용히 공유할 수 있게 한다.

### 진입 조건

| 항목 | 내용 |
| --- | --- |
| Actor | 로그인 사용자 |
| 진입점 | 개인 기록 상세의 `익명으로 나누기`, 익명 나눔 목록 |
| 인증 | 목록 조회는 선택, 공개·취소·반응·신고는 필수 |

### 핵심 정책

- 기본값은 항상 비공개다.
- 사용자가 명시적으로 공개한 기록만 나눔 목록에 노출한다.
- 작성자 이름, 이메일, 프로필 이미지는 노출하지 않는다.
- MVP는 블로그식 목록·상세에 가깝게 만들고, 팔로우·랭킹·실시간 피드는 제외한다.

### API

| 목적 | Method | Path | 인증 |
| --- | --- | --- | --- |
| 기록 공개 | POST | `/api/v1/journals/{id}/share` | 필수 |
| 공개 취소 | DELETE | `/api/v1/journals/{id}/share` | 필수 |
| 나눔 목록 | GET | `/api/v1/shares` | 선택 |
| 좋아요 | POST | `/api/v1/shares/{shareId}/likes` | 필수 |
| 좋아요 취소 | DELETE | `/api/v1/shares/{shareId}/likes` | 필수 |
| 댓글 목록 | GET | `/api/v1/shares/{shareId}/comments` | 선택 |
| 댓글 작성 | POST | `/api/v1/shares/{shareId}/comments` | 필수 |
| 신고 | POST | `/api/v1/shares/{shareId}/reports` | 필수 |

### 화면 구성

| 화면 | 표시 내용 |
| --- | --- |
| 공개 확인 모달 | 공개될 필드, 익명 안내, 취소/공개 버튼 |
| 익명 나눔 목록 | 본문 범위, 일부 묵상 내용, 좋아요 수, 댓글 수 |
| 익명 나눔 상세 | 공유된 묵상, 댓글, 신고 버튼 |

### 공개 데이터

| 항목 | 공개 여부 |
| --- | --- |
| 작성자 식별 정보 | 비공개 |
| 본문 범위 | 공개 |
| 묵상 4필드 | 사용자가 공유한 기록 기준 공개 |
| 작성일 | 공개 가능 |
| 좋아요 수 | 공개 |
| 댓글 | 공개 |

### 예외

| 상황 | 처리 |
| --- | --- |
| 이미 공개된 기록 공개 요청 | 기존 share 정보를 반환하거나 멱등 처리 |
| 공개 취소 후 접근 | 404 또는 비공개 안내 |
| 본인 기록 아님 | 403 처리 |
| 신고 중복 | 중복 신고 안내 또는 멱등 처리 |

### 완료 기준

- 비공개 기록은 나눔 목록에 노출되지 않는다.
- 공개·취소가 즉시 목록에 반영된다.
- 익명 공유 화면에 작성자 정보가 표시되지 않는다.
- 좋아요, 댓글, 신고 최소 기능이 동작한다.

---

## FS-09. 찬양 추천·내 찬양 목록

### 목적

오늘의 QT 흐름 안에서 사용자가 듣고 싶은 찬양을 직접 추가하고 목록처럼 관리한다.

### MVP 범위

| 구분 | 처리 |
| --- | --- |
| 찬양 추천 | 가능하면 구현 |
| 내 찬양 직접 추가 | 가능하면 구현 |
| 재생목록 관리 | 가능하면 구현 |
| 가사 저장 | 금지 |
| 음원 파일 저장 | 금지 |
| 외부 링크 | 허용 |

### 데이터

| 필드 | 설명 |
| --- | --- |
| `title` | 곡 제목 |
| `artist` | 아티스트 |
| `externalUrl` | YouTube 또는 HTTPS 링크 |
| `memo` | 사용자가 남긴 메모 |
| `sortOrder` | 재생목록 정렬 순서 |

### 동작 흐름

1. 오늘의 QT 화면에서 찬양 추천 또는 내 찬양 목록으로 진입한다.
2. 사용자는 곡 제목, 아티스트, 외부 링크를 직접 추가한다.
3. 앱은 가사와 음원 파일을 저장하지 않는다.
4. 사용자는 목록에서 곡 순서를 바꾸거나 삭제한다.

### 예외

| 상황 | 처리 |
| --- | --- |
| 비로그인 | 로그인 유도 |
| 잘못된 URL | 저장 전 검증 메시지 표시 |
| 가사 입력 시도 | 저작권 안내 후 저장 제한 |

### 완료 기준

- 찬양 기능을 구현할 경우 메타데이터와 외부 링크만 저장한다.
- 가사·음원 파일은 저장하지 않는다.

---

## FS-10. 알림·관리자 운영

### 목적

서비스 운영자가 오늘의 QT 본문, 성경 본문, 주석 자료, AI 답변 로그, 익명 나눔 신고를 확인하고 관리할 수 있게 한다. 시연과 운영 확인에 필요한 최소 알림도 함께 제공한다.

### 알림 범위

| 이벤트 | 사용자 알림 |
| --- | --- |
| `journal.created` | 묵상 기록 생성 완료 |
| `journal.creation.failed` | 묵상 기록 생성 실패 |
| `journal.updated` | 필요 시 생략 가능 |
| 신고 접수 | 관리자 확인 대상 |

### API·채널

| 목적 | 채널 | 인증 |
| --- | --- | --- |
| 실시간 알림 | `WS /ws/notifications` | STOMP CONNECT 헤더 JWT |
| 알림 이벤트 | `notification.requested` Kafka | 내부 |
| 관리자 화면 | Flutter Web 또는 별도 웹 관리자 페이지 | 관리자 |
| 관리자 API | BFF 관리자 API가 집계하고 Bible/AI Service를 호출 | 관리자 |

> 관리자 P0 API의 상세 경로와 요청·응답 스키마는 구현 전 `04_API_명세서.md`와 `apis/{service}/openapi.yaml`에 별도로 확정한다.

### 관리자 기능 범위

| 우선순위 | 기능 | 설명 |
| --- | --- | --- |
| P0 | 오늘의 QT 본문 관리 | 날짜별 오늘의 큐티 본문 등록, 수정, 삭제 |
| P0 | 성경 본문 데이터 조회 | 개역한글/KJV 본문 조회 및 데이터 상태 확인 |
| P0 | 주석·자료 데이터 관리 | Matthew Henry 주석, 더미 설명 자료 등록·수정 |
| P0 | AI 답변 로그 조회 | 사용자 질문, 본문 범위, 검색 자료, 프롬프트 요약, 답변, 출처 확인 |
| P0 | 커뮤니티 신고·삭제 | 신고 목록 조회, 익명 나눔 게시글 삭제 또는 비공개 처리 |
| P1 | 사용자 관리 | 사용자 목록, 상태, 권한 확인. 계정 탈퇴 기능은 MVP 제외 |
| P1 | 공지사항 관리 | 홈 또는 공지 영역에 노출할 안내 등록·수정 |
| P1 | 위험 답변 모니터링 | 금지 답변, 위험 답변 탐지 결과 확인 |
| P1 | 감사 로그 | 관리자 작업 이력 조회 |
| P2 | 관리자 권한 관리 | 관리자 역할 부여·회수. v1.1 이후 검토 |
| P2 | 세부 통계 대시보드 | 기간별 통계, 상세 지표 고도화 |
| P2 | 위험 답변 자동 분류 고도화 | AI 로그 기반 자동 분류 정교화 |

### 관리자 기본 지표

| 지표 | 설명 |
| --- | --- |
| 전체 묵상 수 | 생성된 Journal 수 |
| 오늘 묵상 수 | 당일 생성 Journal 수 |
| 인기 구절 Top 10 | 조회 또는 묵상 기준 |
| AI 호출 수 | 세션 또는 turn 기준 |
| 신고 대기 수 | 처리되지 않은 신고 |

### AI 로그 조회 항목

| 항목 | 설명 |
| --- | --- |
| 질문한 사용자 ID | 내부 운영 확인용 식별자 |
| 본문 범위 | 질문이 연결된 성경 본문 |
| 사용자 질문 | 사용자가 입력한 질문 |
| 검색된 자료 목록 | RAG로 검색된 주석·설명 자료 |
| LLM 프롬프트 요약 | 실제 전체 프롬프트가 아니라 운영 확인용 요약 |
| 생성 답변 | AI가 생성한 답변 |
| 출처 목록 | 답변 하단에 표시된 출처 |
| 위험 답변 탐지 결과 | 금지·위험 답변 여부 |
| 생성 시각 | turn 생성 시간 |

### 감사 로그 조회 항목

| 항목 | 설명 |
| --- | --- |
| 관리자 ID | 작업을 수행한 관리자 |
| 수행한 작업 | 생성, 수정, 삭제, 비공개 처리 등 |
| 변경 대상 | QT 본문, 주석, 공유글, 공지 등 |
| 변경 전 값 | 변경 전 데이터 |
| 변경 후 값 | 변경 후 데이터 |
| 작업 시각 | 감사 로그 생성 시간 |

### 완료 기준

- AI 완료 후 묵상 생성 결과를 사용자가 확인할 수 있다.
- 관리자가 P0 항목의 목록 조회와 주요 조치를 수행할 수 있다.
- AI 답변 로그에서 질문, 답변, 출처, 위험 탐지 결과를 확인할 수 있다.
- 신고된 익명 나눔 게시글을 확인하고 삭제 또는 비공개 처리할 수 있다.
- 관리자 작업은 감사 로그로 남긴다.

---

## 4. 핵심 사용자 시나리오

### 4.1 비로그인 첫 진입

1. 앱 실행
2. 온보딩 표시
3. `오늘의 QT 시작하기`
4. 오늘의 본문과 쉬운 설명 확인
5. `AI에게 질문하기` 또는 `묵상 기록하기` 선택
6. 로그인 안내

### 4.2 로그인 사용자 오늘의 QT

1. 홈 진입
2. 오늘의 QT 시작
3. 한글·영어 본문 읽기
4. 설명 또는 주석 선택 열람
5. AI 질문
6. 답변과 출처 확인
7. 묵상 완료
8. DRAFT 묵상 기록 생성
9. 4개 필드 작성 후 저장

### 4.3 묵상 기록 익명 나눔

1. 내 묵상 기록 상세 진입
2. `익명으로 나누기` 선택
3. 공개 안내 확인
4. 공개 처리
5. 익명 나눔 목록에 노출
6. 필요 시 공개 취소

---

## 5. API 매핑 요약

| 기능 | Method | Path | OpenAPI |
| --- | --- | --- | --- |
| 오늘의 QT | GET | `/api/v1/qt/today` | `apis/bff/openapi.yaml` |
| 입체 본문 | GET | `/api/v1/passages/{bookCode}/{chapter}/{verse}` | `apis/bff/openapi.yaml` |
| 쉬운 설명 | GET | `/api/v1/explanations/{bookCode}/{ch}/{v}` | `apis/bible/openapi.yaml` |
| 주석 | GET | `/api/v1/commentary/{bookCode}/{ch}/{v}` | `apis/bible/openapi.yaml` |
| AI 세션 생성 | POST | `/ai/sessions` | `apis/ai/openapi.yaml` |
| AI 질문 SSE | POST | `/ai/sessions/{id}/turns` | `apis/ai/openapi.yaml` |
| AI 묵상 완료 | POST | `/ai/sessions/{id}/complete` | `apis/ai/openapi.yaml` |
| 묵상 목록 | GET | `/api/v1/journals` | `apis/bible/openapi.yaml` |
| 묵상 상세 | GET | `/api/v1/journals/{id}` | `apis/bible/openapi.yaml` |
| 묵상 수정 | PATCH | `/api/v1/journals/{id}` | `apis/bible/openapi.yaml` |
| 묵상 삭제 | DELETE | `/api/v1/journals/{id}` | `apis/bible/openapi.yaml` |
| 나눔 공개 | POST | `/api/v1/journals/{id}/share` | `apis/bible/openapi.yaml` |
| 나눔 취소 | DELETE | `/api/v1/journals/{id}/share` | `apis/bible/openapi.yaml` |
| 나눔 목록 | GET | `/api/v1/shares` | `apis/bible/openapi.yaml` |
| 좋아요 | POST/DELETE | `/api/v1/shares/{shareId}/likes` | `apis/bible/openapi.yaml` |
| 댓글 | GET/POST | `/api/v1/shares/{shareId}/comments` | `apis/bible/openapi.yaml` |
| 신고 | POST | `/api/v1/shares/{shareId}/reports` | `apis/bible/openapi.yaml` |

---

## 6. 이벤트 매핑 요약

| 이벤트 | Producer | Consumer | 기능 연결 |
| --- | --- | --- | --- |
| `user.activity.tracked` | bff-aggregator | bible-service | 본문 조회 이력 |
| `ai.session.completed` | ai-service | bible-service | 묵상 DRAFT 생성 |
| `journal.created` | bible-service | bff-aggregator 등 | 묵상 생성 알림 |
| `journal.updated` | bible-service | bff-aggregator 등 | 묵상 수정 알림 |
| `journal.deleted` | bible-service | bff-aggregator 등 | 묵상 삭제 알림 |
| `journal.creation.failed` | bible-service | bff-aggregator 등 | 묵상 생성 실패 알림 |
| `notification.requested` | 다수 | bff-aggregator | WebSocket 알림 전송 |

---

## 7. 테스트 관점

| 기능 | 필수 테스트 |
| --- | --- |
| 온보딩 | 첫 실행 표시, 완료 후 재표시 방지 |
| 소프트 로그인 | 비로그인 본문 조회 가능, 보호 기능 로그인 유도 |
| 입체 본문 | 한/영 절 매칭, 일부 실패 fallback |
| 설명 | 데이터 없음 상태, 공개 접근 |
| 주석 | 로그인 필요, 선택 로딩 |
| AI Q&A | SSE 이벤트 순서, 출처 표시, 중단 재시도 |
| AI 완료 | `ai.session.completed` 발행, DRAFT 생성 |
| 묵상 기록 | 4필드 수정, 본인 권한, 기본 비공개 |
| 익명 나눔 | 공개/취소, 작성자 비노출, 좋아요/댓글/신고 |
| 저작권 | 금지 번역본·가사·음원 저장 차단 |

---

## 8. MVP 완료 체크리스트

- [ ] 비로그인 사용자가 온보딩과 오늘의 QT 본문을 볼 수 있다.
- [ ] 로그인 사용자가 주석과 AI 질문을 사용할 수 있다.
- [ ] AI 답변은 SSE로 스트리밍되고 출처가 표시된다.
- [ ] 사용자가 `묵상 완료`를 누르면 DRAFT 묵상 기록이 생성된다.
- [ ] 묵상 기록은 `felt`, `memorableVerse`, `application`, `prayer` 4필드로 저장된다.
- [ ] 묵상 기록은 기본 `PRIVATE`다.
- [ ] 사용자가 선택한 기록만 `ANONYMOUS_SHARED`로 공개된다.
- [ ] 익명 나눔에서 작성자 식별 정보가 노출되지 않는다.
- [ ] 개역개정, ESV, NIV, 찬양 가사·음원 저장을 하지 않는다.
- [ ] OpenAPI 명세와 구현 필드명이 일치한다.

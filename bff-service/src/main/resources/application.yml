spring:
  application:
    name: bff-service
  profiles:
    active: local

server:
  port: 8083

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  tracing:
    sampling:
      probability: 1.0
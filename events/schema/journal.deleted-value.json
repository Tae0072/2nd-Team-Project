{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "journal.deleted-value",
  "title": "JournalDeletedEvent",
  "description": "묵상 노트 삭제 이벤트 — Kafka topic: journal.deleted",
  "type": "object",
  "required": ["eventId", "eventType", "occurredAt", "idempotencyKey", "payload"],
  "properties": {
    "eventId": { "type": "string", "format": "uuid" },
    "eventType": { "type": "string", "const": "JOURNAL_DELETED" },
    "occurredAt": { "type": "string", "format": "date-time" },
    "idempotencyKey": { "type": "string" },
    "payload": {
      "type": "object",
      "required": ["journalId", "userId"],
      "properties": {
        "journalId": { "type": "integer" },
        "userId": { "type": "integer" },
        "deletedAt": { "type": "string", "format": "date-time" }
      }
    }
  }
}